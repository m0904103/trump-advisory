import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/trump-advisory/',
  // Default build settings with hashing enabled to prevent caching
})
